/* 
 * Classe tournant autour de la génération des niveaux
 */
class Dungeon {
    constructor(){
        
    };
    generate(onMap = false, mode = "default") {
        if(onMap) {
            
        }
        else {
            console.log("il faut spécifier un tableau !");
        }
    };
    addToMap(onMap) {
        
    }
}

